import React from "react";
import { GoogleMap, Marker } from "react-google-maps";

const Map: any = ({xCoord}: any, {yCoord}: any) => { 
    return (
        <div>

        </div>
    );
}

export default Map;

